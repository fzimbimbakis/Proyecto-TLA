int main(){

    int base = 2;
    int iteraciones = 15;

    if(iteraciones > 0){
        while ( iteraciones != 0 ){
            base = base + base;
            iteraciones = iteraciones - 1;
        }
    }
    printf("2 elevado a la 16: %d\n" ,base);
    return base;
}